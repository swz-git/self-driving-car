[package]
name = "collect"
version = "0.1.0"
authors = ["John Simon <john@whatisaph.one>"]
edition = "2018"

[dependencies]
csv = "1.0.2"
flatbuffers = "0.5.0"
ordered-float = "1.0.1"

nalgebra.workspace = true

rlbot.workspace = true

common = { path = "../common" }

[features]
strict = []
